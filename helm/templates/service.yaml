apiVersion: v1
kind: Service
metadata:
  name: {{ include "aiops-quality-service.fullname" . }}
  labels:
    app: {{ include "aiops-quality-service.name" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "aiops-quality-service.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
      protocol: TCP
      name: http