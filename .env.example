# === Container registry credentials ===
# GitLab Container Registry URL, e.g. registry.gitlab.com/group/project
REGISTRY=registry.gitlab.com/goit-neo/mlops-final-project
# Full image repository for the inference service
IMAGE_REPOSITORY=registry.gitlab.com/goit-neo/mlops-final-project/aiops-quality-service
# Image tag used in Helm values (override per release)
IMAGE_TAG=202409010930

# === GitLab project automation ===
# Numeric GitLab project ID
GITLAB_PROJECT_ID=12345678
# Pipeline trigger token configured for retrain-model job
GITLAB_TRIGGER_TOKEN=glpt-xxxxxxxxxxxxxxxx
# Git repository URL ArgoCD should sync
GIT_REPO_URL=https://gitlab.com/goit-neo/mlops-final-project.git

# === ArgoCD access ===
ARGOCD_SERVER=localhost:8080
ARGOCD_USERNAME=admin
ARGOCD_PASSWORD=123456